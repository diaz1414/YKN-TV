name = "ykn-proxy"
main = "worker.js"
compatibility_date = "2024-09-23"

# Worker runs on Cloudflare's free tier
# Free: 100,000 req/day | CPU: 10ms per request (more than enough for proxying)
# Paid ($5/mo): 10 million req/month — recommended if you have 1000+ users

[observability]
enabled = true
